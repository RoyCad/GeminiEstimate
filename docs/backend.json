
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the GeminiEstimate application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user, same as Firebase Auth UID."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "User's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile photo.",
          "format": "uri"
        },
        "creationTime": {
          "type": "string",
          "description": "Timestamp of when the user account was created.",
          "format": "date-time"
        },
        "role": {
          "type": "string",
          "description": "The role of the user, can only be 'Admin' or 'Client'.",
          "enum": [
            "Admin",
            "Client"
          ]
        }
      },
      "required": [
        "id",
        "email",
        "role"
      ]
    },
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "description": "Represents a construction project created by an Admin for a Client.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the project."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User (Client) who owns the project."
        },
        "projectNumber": {
          "type": "string",
          "description": "Custom identifier for the project (e.g., AIS/101)."
        },
        "projectName": {
          "type": "string",
          "description": "Name of the project."
        },
        "autoGeneratedDescription": {
          "type": "string",
          "description": "An auto-generated description based on the selected services."
        },
        "clientName": {
          "type": "string",
          "description": "Name of the client for this project."
        },
        "clientEmail": {
          "type": "string",
          "description": "Email of the client for this project.",
          "format": "email"
        },
        "clientPhone": {
          "type": "string",
          "description": "Contact phone number of the client."
        },
        "projectAddress": {
          "type": "string",
          "description": "Contact address of the client."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the project was created.",
          "format": "date-time"
        },
        "numberOfStories": {
            "type": "number",
            "description": "The total number of stories or floors for the building (e.g., 5 for G+4)."
        },
        "services": {
          "type": "array",
          "description": "List of services selected for the project.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "label": { "type": "string" },
              "selected": { "type": "boolean" }
            }
          }
        },
        "billing": {
            "type": "object",
            "description": "Billing details for the project services.",
            "properties": {
                "type": { "type": "string", "enum": ["sqft", "package"] },
                "ratePerSqft": { "type": "number" },
                "totalSqft": { "type": "number" },
                "packageAmount": { "type": "number" },
                "soilTestFee": { "type": "number" }
            }
        }
      },
      "required": [
        "id",
        "userId",
        "projectNumber",
        "projectName",
        "clientName",
        "clientEmail",
        "createdAt",
        "numberOfStories"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "Stores user profile and role information."
        }
      },
      {
        "path": "/projects/{projectId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/Project"
          },
          "description": "Stores all projects. Admins create projects for clients."
        }
      }
    ],
    "reasoning": "The Firestore data structure is reset to a minimal state, containing only users and projects collections. This provides a clean slate for starting fresh with the application, removing all previous data structures like transactions, materials, and proposals."
  }
}
